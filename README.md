The project was written using Django ORM. 
To start the project, install all necessary plugins (reqrements.txt) and run the main.py file
All functions that the program can perform are recorded in the menu.
